import type { NextPage } from "next";
import style from "../styles/Home.module.scss";

const Palletes: NextPage = (props: any) => {
  return (
    <div className={style.maincontainer}>
      <h2>color Palletes Page</h2>
    </div>
  );
};

export default Palletes;
